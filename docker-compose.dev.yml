services:
  backend:
    build:
      context: ./backend_python
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend_python/app:/app/app
      - ./runtime:/app/runtime
